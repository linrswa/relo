# Greeting library PRD

Complete the small Go greeting library in two stages. First normalize user names by trimming surrounding whitespace. Then produce `Hello, NAME!` from the normalized name and reject an empty normalized name. The complete test suite must pass before the library is considered stable.
