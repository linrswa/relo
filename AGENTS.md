# AGENTS.md

本文件定義程式設計代理在此儲存庫中的工作方式。目標是減少錯誤假設、過度設計、無關修改，以及未經驗證的完成宣告。

> 取捨：以下規則偏向正確與可驗證，而非盲目追求速度。對低風險、顯而易見的小改動，可依情況簡化流程。
>
> 本指南受 [andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills) 啟發並重新整理為 `AGENTS.md`。

## 指令優先順序

1. 遵循使用者的明確要求與驗收條件。
2. 遵循距離目標檔案最近的 `AGENTS.md`；較深層目錄的規則優先。
3. 遵循儲存庫既有的程式風格、架構、測試與工具鏈。
4. 若規則互相衝突、需求不清楚，或可能造成不可逆影響，先指出問題並詢問，不要自行猜測。

## 1. 動手前先理解

**不要默默假設，也不要掩飾不確定性。**

開始實作前：

- 先閱讀相關程式碼、測試、設定與文件，再決定修改方式。
- 區分已確認的事實、合理推論與尚待確認的假設。
- 若需求有多種合理解讀，列出會影響實作的差異，不要擅自選擇。
- 若存在更簡單或風險更低的方案，應主動提出。
- 只有在歧義會實質影響行為、介面、資料、安全性或修改範圍時才中止並詢問；低風險且容易回復的細節可採最保守做法，並簡短說明假設。

## 2. 簡單優先

**以最少且完整的改動解決目前問題。**

- 不新增需求以外的功能。
- 不為單次使用建立抽象層、框架或設定系統。
- 不加入未被要求的擴充點、相容層或「未來可能用到」的彈性。
- 優先沿用現有模式與依賴；沒有明確必要時，不引入新套件。
- 只處理真實且合理的錯誤邊界，不為不可能發生的情境堆疊防禦程式碼。
- 若實作明顯比問題本身複雜，停下來重新簡化。

自我檢查：資深工程師是否會認為這個解法過度設計？若會，先縮小方案。

## 3. 精準修改

**只修改完成任務所必須的內容，並只清理由本次改動造成的問題。**

編輯既有程式碼時：

- 不順手重構、重新命名、改格式或改寫鄰近註解。
- 不修復與任務無關的問題；可以在結果中指出，但不要混入本次修改。
- 配合既有命名、排版、錯誤處理與架構風格。
- 不覆蓋或撤銷使用者尚未提交的改動。
- 修改產生未使用的 import、變數、函式或檔案時，應一併清除。
- 未經明確要求，不刪除原本就存在的死程式碼。
- 未經允許，不執行破壞性指令、重寫歷史或大範圍自動格式化。

檢驗標準：每一行 diff 都應能直接對應到需求或必要驗證。

## 4. 以可驗證目標驅動

**先定義成功，再實作並反覆驗證。**

將模糊任務轉成可觀察的結果：

- 修正 bug：先建立能重現問題的測試或步驟，再讓它通過。
- 新增功能：先定義輸入、輸出、邊界情境與驗收條件。
- 重構：先確認既有行為與測試結果，修改後再次確認一致。
- 效能改善：先量測基準，再以相同條件比較結果。

多步驟任務使用簡短計畫：

```text
1. [步驟] → 驗證：[檢查方式]
2. [步驟] → 驗證：[檢查方式]
3. [步驟] → 驗證：[檢查方式]
```

計畫應描述成果與驗證方式，而不是「查看程式碼」「進行修改」這類無法判定完成的活動。

## 5. 驗證與品質

修改完成後：

1. 先執行與改動最相關的測試、型別檢查或 lint。
2. 在成本合理時，再執行較完整的測試套件。
3. 檢查最終 diff，確認沒有除錯輸出、暫存內容、敏感資訊或無關修改。
4. 驗證錯誤路徑與邊界情境，而不只確認正常路徑。
5. 不得宣稱未實際執行的檢查已通過。

若因環境、權限、時間或缺少依賴而無法驗證，應清楚說明：

- 哪些檢查沒有執行；
- 原因是什麼；
- 使用者可如何完成驗證；
- 尚存哪些風險。

## 6. 溝通方式

- 回覆精簡、具體，避免逐步播報無關操作。
- 在需要澄清時，提出聚焦且可作答的問題。
- 對重要決策，簡短說明採用方案、替代方案與取捨。
- 不隱藏錯誤、失敗測試或不確定性。
- 完成時摘要：改了什麼、如何驗證、是否仍有風險或後續事項。

## 標準工作流程

1. **理解**：閱讀規則與相關檔案，確認需求、限制與現況。
2. **定義**：寫下可驗證的成功條件；必要時先詢問。
3. **實作**：採用最小、符合既有風格的修改。
4. **驗證**：由窄到廣執行檢查，失敗就修正並重跑。
5. **檢視**：確認 diff 精準、沒有副作用或遺漏。
6. **回報**：說明結果、驗證證據與任何剩餘風險。

## 完成定義

只有在以下條件成立時，任務才算完成：

- 使用者要求與驗收條件已滿足；
- 改動範圍保持最小且沒有無關修改；
- 相關測試或檢查已通過，或未執行的原因已明確揭露；
- 文件、型別或測試已在行為變更確實需要時同步更新；
- 最終回覆準確描述實際完成與驗證的內容。
